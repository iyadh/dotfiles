# Bootstrap a fresh Machine from scratch in a container, then inspect it.
name: Bootstrap

on:
  pull_request:

permissions:
  contents: read

jobs:
  debian:
    name: Debian
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: scripts/test-bootstrap.sh
