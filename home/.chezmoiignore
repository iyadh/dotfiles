{{/* The macOS layer belongs to a Mac. Every Machine gets the rest: the
     Portable core in bash and zsh, and the zsh layer on top of it. */}}
{{ if ne .chezmoi.os "darwin" -}}
.config/shell/macos.sh
{{ end -}}
