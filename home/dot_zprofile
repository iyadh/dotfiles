# Fig pre block. Keep at the top of this file.
[[ -f "$HOME/.fig/shell/zprofile.pre.zsh" ]] && builtin source "$HOME/.fig/shell/zprofile.pre.zsh"
eval "$(/opt/homebrew/bin/brew shellenv)"


# Fig post block. Keep at the bottom of this file.
[[ -f "$HOME/.fig/shell/zprofile.post.zsh" ]] && builtin source "$HOME/.fig/shell/zprofile.post.zsh"


# Added by Toolbox App
export PATH="$PATH:/Users/iyadh/Library/Application Support/JetBrains/Toolbox/scripts"


# Created by `pipx` on 2024-11-29 14:46:47
export PATH="$PATH:/Users/iyadh/.local/bin"


# Added by Antigravity CLI installer
export PATH="/Users/iyadh/.local/bin:$PATH"

# >>> Codex installer >>>
export PATH="/Users/iyadh/.local/bin:$PATH"
# <<< Codex installer <<<

# Local override, never tracked: work and per-Machine settings. Last, so it wins.
[[ -f "$HOME/.zprofile.local" ]] && source "$HOME/.zprofile.local"
